{
  "name": "whatsapp-flows-endpoint",
  "version": "1.0.0",
  "description": "WhatsApp Flows Encrypted Endpoint",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "gen-keys": "node src/keyGenerator.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}